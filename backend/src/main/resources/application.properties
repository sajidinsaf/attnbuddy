spring.application.name=attnbuddy

# JWT
jwt.secret=${JWT_SECRET:attnbuddy-dev-secret-must-be-at-least-256-bits-long-change-in-production!!}
jwt.access-token-expiry=3600000
jwt.refresh-token-expiry=2592000000

# JPA
spring.jpa.open-in-view=false

# Default to local profile for development
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}
